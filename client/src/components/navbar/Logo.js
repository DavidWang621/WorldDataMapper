import React from 'react';

const Logo = (props) => {
    return (
        <div className='logo' style={{color: "red"}}>
            The World Data Mapper
        </div>
    );
};

export default Logo;